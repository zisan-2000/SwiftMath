import { AppPageSkeleton } from "@/components/loading/page-skeleton";

/** Shell-shaped skeleton shown while the account route segment streams in. */
export default function AccountLoading() {
  return <AppPageSkeleton />;
}
