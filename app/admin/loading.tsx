import { AppPageSkeleton } from "@/components/loading/page-skeleton";

/** Shell-shaped skeleton shown while an admin route segment streams in. */
export default function AdminLoading() {
  return <AppPageSkeleton />;
}
