import { AppPageSkeleton } from "@/components/loading/page-skeleton";

/** Shell-shaped skeleton shown while a teacher route segment streams in. */
export default function TeacherLoading() {
  return <AppPageSkeleton />;
}
