import { AppPageSkeleton } from "@/components/loading/page-skeleton";

/** Shell-shaped skeleton shown while a super-admin route segment streams in. */
export default function SuperLoading() {
  return <AppPageSkeleton />;
}
