import { AppPageSkeleton } from "@/components/loading/page-skeleton";

/** Shell-shaped skeleton shown while a student route segment streams in. */
export default function StudentLoading() {
  return <AppPageSkeleton />;
}
