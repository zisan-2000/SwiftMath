import { PracticeFocusSkeleton } from "@/components/loading/page-skeleton";

/** Focus-shell skeleton shown while a practice session loads. */
export default function PracticeSessionLoading() {
  return <PracticeFocusSkeleton />;
}
